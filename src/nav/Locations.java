package nav;

/**
 * This enum holds all the location types. (environemnts only)
 * Subject to change.
 * @author Jon
 */
public enum Locations {
	CAVE, 
	PLAINS, 
	SWAMP,
	FOREST,
	MOUNTAINS,
	ROAD,
	WALL,  // to exit.
	STAIRS // also to exit. 
}